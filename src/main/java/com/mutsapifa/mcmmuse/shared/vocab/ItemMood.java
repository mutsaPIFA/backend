package com.mutsapifa.mcmmuse.shared.vocab;

/**
 * 옷장 아이템 무드 태그 — 계약 api-v1.md "공통 타입"과 1:1.
 *
 * <p>⚠️ 큐레이터의 {@code Mood}(무드 선택, styling BC 엔티티·시드 6개)와 다르다.
 */
public enum ItemMood {
  미니멀,
  캐주얼,
  클래식,
  스트릿,
  페미닌,
  럭셔리
}
